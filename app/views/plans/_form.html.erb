<%= form_with(model: plan, local: true) do |form| %>
  <% if plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
      <% plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, id: :plan_name %>
    </div>

    <div class="field">
      <%= form.label :duration %>
      <%= form.datetime_select :duration, id: :plan_duration, class: :datepicker %>
    </div>

    <div class="field">
      <%= form.label :price %>
      <%= form.text_field :price, id: :plan_price %>
    </div>

    <div class="field">
      <%= form.label :currency %>
      <%= form.text_field :currency, id: :plan_current %>
    </div>
  </div>
  <br>
  <div class="form">
    <div class="actions">
      <%= form.submit %>
    </div>
  </div>

<% end %>
