<li>
  <%= link_to resume_path(all_resume) do %>
  <%= image_tag placeholder_img(all_resume.profile_pic, 'logo'), width: '100%', alt: "" %>
  <div class="resumes-list-content">
    <h4><%= all_resume.first_name %> <%= all_resume.last_name %> <span><%= all_resume.desired_job_title %></span></h4>
    <span><i class="fa fa-map-marker"></i> <%= all_resume.city %></span>
    <span><i class="fa fa-money"></i> $<%= all_resume.desired_salary_per_month %> / month</span>
    <p>Over 8000 hours on oDesk (only Drupal related). Highly motivated, goal-oriented, hands-on senior software engineer with extensive technical skills and over 15 years of experience in software development</p>

    <div class="skills">
      <% all_resume.skills.each do |skill| %>
      <span><%= skill.title %></span>
      <% end %>
    </div>
    <div class="clearfix"></div>

  </div>
  <% end %>
  <div class="clearfix"></div>
</li>        


<!--
        <% if user_signed_in? %>
          <% if current_user&.bookmark_resume?(all_resume) %>
          <%= link_to '', unbookmark_resume_path(resume_id: all_resume.id), class: 'fa fa-star fa-2x unbookmark small', title: 'Unbookmark!', method: :delete %>
        <% else %>
          <%= link_to '', bookmark_resume_path(resume_id: all_resume.id), class: 'fa fa-star fa-2x bookmark small', alt: 'Bookmark!', title: 'Bookmark!' %>
          <% end %>
        <% end %>
-->